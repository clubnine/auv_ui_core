/// 按钮类型枚举
enum AuvWidgetType { primary, secondary, success, warning, danger, info }

/// 按钮尺寸枚举
enum AuvWidgetSize {
  micro, // 超小尺寸
  tiny, // 极小尺寸
  mini, // 迷你尺寸
  small, // 小尺寸
  medium, // 中等尺寸
  large, // 大尺寸
}

/// 按钮形状枚举
enum AuvWidgetShape { circle, radius }
